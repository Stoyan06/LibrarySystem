@page
<title>Предупреждение</title>
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-12 col-md-8 col-lg-6">
            <div class="card border-danger shadow">
                <div class="card-header bg-danger text-white">
                    <h1 class="text-center mb-0 fs-4">
                        <i class="bi bi-exclamation-triangle-fill me-2"></i>
                        Невалидна операция
                    </h1>
                </div>
                <div class="card-body p-4 text-center">
                    <div class="mb-4">
                        <div class="alert-icon mb-3">
                            <i class="bi bi-x-octagon-fill text-danger" style="font-size: 3rem;"></i>
                        </div>
                        <div class="alert alert-danger py-3">
                            <p class="fw-bold mb-0">
                                Тази библиотечна единица е бракувана!
                            </p>

                            <p class="mb-0">
                                Операцията не може да бъде извършена върху бракуван екземпляр.
                            </p>
                        </div>
                    </div>
                    <div class="mt-4 d-flex justify-content-center flex-wrap gap-2">
                        <a href="javascript:void(0);" onclick="history.back();" class="btn btn-secondary">
                            <i class="bi bi-arrow-left me-2"></i>
                            Назад
                        </a>

                    </div>
                </div>
                <div class="card-footer bg-light text-center p-2">
                    <small class="text-muted">
                        <i class="bi bi-info-circle me-1"></i>
                        Бракуваните библиотечни единици не могат да бъдат заемани или редактирани.
                    </small>
                </div>
            </div>
        </div>
    </div>
</div>
@section Styles {
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        /* Animation for alert icon */
        .alert-icon {
            animation: pulse 2s infinite;
        }

        @@keyframes pulse {
            0% {
                transform: scale(1);
            }

            50% {
                transform: scale(1.1);
            }

            100% {
                transform: scale(1);
            }
        }
        /* Card styling */
        .card {
            border-radius: 12px;
            overflow: hidden;
            border-width: 2px;
        }
        /* Button styling for better responsiveness */
        @@media (max-width: 575.98px) {
            .btn {
                width: 100%;
                margin-bottom: 0.5rem;
            }
        }
    </style>
}